name: Deploy and Promote Domain

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate Vercel token
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          echo "🔐 Validating VERCEL_TOKEN..."
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "ERROR: VERCEL_TOKEN is not set (check repository secrets)" >&2
            exit 1
          fi
          # will print who the token belongs to or exit non-zero if invalid
          npx vercel whoami --token "$VERCEL_TOKEN"

      - name: Validate NEXT_PUBLIC_SANITY_DATASET
        env:
          NEXT_PUBLIC_SANITY_DATASET: ${{ vars.NEXT_PUBLIC_SANITY_DATASET }}
        run: |
          echo "🛠 Validating NEXT_PUBLIC_SANITY_DATASET availability..."
          if [ -z "${NEXT_PUBLIC_SANITY_DATASET+x}" ]; then
            echo "::error::NEXT_PUBLIC_SANITY_DATASET is UNSET (no repository variable found)."
            exit 1
          fi
          if [ -z "$NEXT_PUBLIC_SANITY_DATASET" ]; then
            echo "::error::NEXT_PUBLIC_SANITY_DATASET is defined but EMPTY."
            exit 1
          fi
          echo "::notice::NEXT_PUBLIC_SANITY_DATASET is set (value: ${NEXT_PUBLIC_SANITY_DATASET})."

      - name: Deploy to Vercel
        id: deploy
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          NEXT_PUBLIC_SANITY_DATASET: ${{ vars.NEXT_PUBLIC_SANITY_DATASET }}
          VERCEL_ORG_ID: ${{ vars.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ vars.VERCEL_PROJECT_ID }}
          PROJECT_NAME: ${{ vars.VERCEL_PROJECT_NAME || vars.PROJECT_NAME }}
        run: |
          echo "🚀 Deploying to Vercel (non-interactive)..."
          # Use both VERCEL_ORG_ID and VERCEL_PROJECT_ID to ensure correct project
          VERCEL_ORG_ID="${VERCEL_ORG_ID}"
          VERCEL_PROJECT_ID="${VERCEL_PROJECT_ID}"
          export VERCEL_ORG_ID VERCEL_PROJECT_ID

          PROJECT_NAME=${PROJECT_NAME:-shivansh-portfolio}
          
          # Deploy directly since vercel.json provides explicit project configuration
          DEPLOY_OUTPUT=$(npx vercel --token "$VERCEL_TOKEN" --prod --yes --env NEXT_PUBLIC_SANITY_DATASET="$NEXT_PUBLIC_SANITY_DATASET" --env SANITY_STUDIO_DATASET="$NEXT_PUBLIC_SANITY_DATASET")
          VERCEL_STATUS=$?
          if [ "$VERCEL_STATUS" -ne 0 ]; then
            echo -e "::error::Vercel deploy failed with exit code $VERCEL_STATUS. Output:\n$DEPLOY_OUTPUT" >&2
            exit "$VERCEL_STATUS"
          fi
          echo "$DEPLOY_OUTPUT"
          
          # Extract deployment URL from Vercel output
          DEPLOY_URL=$(echo "$DEPLOY_OUTPUT" | grep -oE "https://${PROJECT_NAME}-[^[:space:]]+\.vercel\.app" | head -1)
          
          # Fallback: extract any Vercel deployment URL if the pattern doesn't match
          if [ -z "$DEPLOY_URL" ]; then
            DEPLOY_URL=$(echo "$DEPLOY_OUTPUT" | grep -oE 'https://[^[:space:]]+\.vercel\.app' | grep -E "${PROJECT_NAME}" | head -1)
          fi
          
          if [ -z "$DEPLOY_URL" ]; then
            echo "::error::Could not extract deployment URL from Vercel output"
            exit 1
          fi
          
          echo "deployment_url=$DEPLOY_URL" >> $GITHUB_OUTPUT
          echo "✅ Deployment URL captured: $DEPLOY_URL"

      - name: Verify production deployment
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          echo "✅ Deployment already promoted to production via --prod flag"
          echo "🔗 Production URL: https://shivansh-sharma.vercel.app"
          echo "📊 Deployment URL: ${{ steps.deploy.outputs.deployment_url }}"

    permissions:
      contents: read
      id-token: write
